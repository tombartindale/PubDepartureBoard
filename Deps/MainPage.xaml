<Page
    x:Class="Deps.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="Black" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView x:Name="alltrains" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollMode="Disabled" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch">
            <ListView.ItemContainerTransitions>
                <TransitionCollection/>
            </ListView.ItemContainerTransitions>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock  VerticalAlignment="Top" FontSize="34" Text="{Binding std}" FontFamily="Score Board" Foreground="#FFFFF974" Margin="0,0,10,0"></TextBlock>
                        <!--<Rectangle Fill="Red" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Rectangle>-->
                        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Stretch">
                            <TextBlock FontSize="34" Text="{Binding destination.location.locationName}" FontFamily="Score Board" Foreground="#FFFFF974"></TextBlock>
                            <TextBlock  FontSize="20" Text="{Binding subsequentCallingPoints}" FontFamily="Score Board" Foreground="#FFFFF974" HorizontalAlignment="Stretch" TextWrapping="WrapWholeWords" TextTrimming="CharacterEllipsis"></TextBlock>
                        </StackPanel>
                        <TextBlock Grid.Column="2" TextAlignment="Right" Text="{Binding IsLate}" FontFamily="Score Board" Foreground="#FFA8A8A8"></TextBlock>
                        <TextBlock Grid.Column="2" TextAlignment="Right" Text="{Binding IsMissed}" FontFamily="Score Board" Foreground="#FFA8A8A8"></TextBlock>
                        <TextBlock Grid.Column="2" TextAlignment="Right" Text="{Binding IsRunNow}" FontFamily="Score Board" Foreground="#FFA8A8A8"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
        <TextBlock Grid.Row="1" x:Name="updatedat" FontFamily="Score Board" Text="{Binding}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="#528626"></TextBlock>
    </Grid>
</Page>
